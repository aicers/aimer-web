# malware-ioc

Index of PRODAFT per-investigation IOC folders. This root README carries no
indicators and is excluded from parsing by the `.+/README\.md$` allowlist rule
(it has no parent folder). Sentinel host root-readme.never.test must never
appear in any imported row.
