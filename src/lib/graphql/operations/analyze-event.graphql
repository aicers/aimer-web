mutation AnalyzeEvent(
  $event: String!
  $timestamp: StringNumber!
  $name: String!
  $model: String!
  $lang: Language
) {
  analyzeEvent(
    event: $event
    timestamp: $timestamp
    name: $name
    model: $model
    lang: $lang
  ) {
    threatScore
    analysis
  }
}
