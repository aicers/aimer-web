## Client-side endpoint: for best compatibility, use the built-in proxy
NEXT_PUBLIC_GRAPHQL_ENDPOINT=/api/graphql

## Server-side upstream endpoint (the real GraphQL URL)
AIMER_GRAPHQL_ENDPOINT=https://api.example.com/graphql

## Optional: allow self-signed certs in server proxy (local only)
# INSECURE_TLS=1
