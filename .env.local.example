# Local development (.env.local)
# Next.js loads this file automatically when running `npm run dev`.

# Client calls the built-in proxy
NEXT_PUBLIC_GRAPHQL_ENDPOINT=/api/graphql

# Upstream GraphQL endpoint for the server-side proxy
AIMER_GRAPHQL_ENDPOINT=https://api.example.com/graphql

# Optional: allow self-signed certs in local only
# INSECURE_TLS=1

