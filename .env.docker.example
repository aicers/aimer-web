# Environment variables used when running via Docker Compose

# Client calls the built-in proxy
NEXT_PUBLIC_GRAPHQL_ENDPOINT=/api/graphql

# Upstream GraphQL endpoint for the server-side proxy
# If your GraphQL server runs on the host, use host.docker.internal
# (Linux requires extra_hosts in compose, already configured)
AIMER_GRAPHQL_ENDPOINT=https://host.docker.internal:8445/graphql

# Optional (local only): accept self-signed upstream certs in server proxy
# INSECURE_TLS=1
